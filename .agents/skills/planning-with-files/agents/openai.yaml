interface:
  display_name: "Planning With Files"
  short_description: "Keep opt-in task memory on disk"
  default_prompt: "Use $planning-with-files to create concise, persistent task memory for this task."
policy:
  allow_implicit_invocation: false
