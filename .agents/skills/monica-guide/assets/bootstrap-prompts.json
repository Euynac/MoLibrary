{
  "schemaVersion": 1,
  "repository": "Tairitsua/Monica",
  "skill": "monica-guide",
  "immutableRef": "{{MONICA_IMMUTABLE_REF}}",
  "locales": {
    "en-US": {
      "codex": "Install only the named monica-guide skill globally from https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide with `npx --yes skills@1.5.21 add https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide -g -a codex -s monica-guide -y`. Verify installed discovery with `npx --yes skills@1.5.21 ls -g -a codex`. Codex normally discovers skills live; restart only if monica-guide is still unavailable. Then use $monica-guide to run `init --release-tag {{MONICA_IMMUTABLE_REF}} --agent codex` for this repository, show the complete dry-run preview and planDigest, and make no downstream changes until I approve that preview. If Guide changes AGENTS.md, start a new agent run before development work because instructions load per run.",
      "claude": "Install only the named monica-guide skill globally from https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide with `npx --yes skills@1.5.21 add https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide -g -a claude-code -s monica-guide -y`. Verify installed discovery with `npx --yes skills@1.5.21 ls -g -a claude-code`. Claude Code normally live-watches existing skill directories; restart only if monica-guide is unavailable or the top-level skills directory did not exist when this session began. Then use $monica-guide to run `init --release-tag {{MONICA_IMMUTABLE_REF}} --agent claude-code` for this repository, show the complete dry-run preview and planDigest, and make no downstream changes until I approve that preview. If Guide changes CLAUDE.md or AGENTS.md, start a new session before development work because instructions load at session start.",
      "generic": "Install only the named monica-guide skill globally from the immutable source https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide by using `npx --yes skills@1.5.21 add https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide -g -a codex -a claude-code -s monica-guide -y`. Verify source discovery with the same URL and `--list`, then verify each installed target with `npx --yes skills@1.5.21 ls -g -a <agent>`. Restart only if the host cannot discover the new skill. Invoke $monica-guide `init --release-tag {{MONICA_IMMUTABLE_REF}} --agent codex --agent claude-code` for this repository, present its complete dry-run preview and planDigest, and wait for approval before applying it or making downstream changes."
    },
    "zh-CN": {
      "codex": "使用 `npx --yes skills@1.5.21 add https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide -g -a codex -s monica-guide -y`，仅从不可变来源 https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide 全局安装具名 monica-guide Skill。使用 `npx --yes skills@1.5.21 ls -g -a codex` 验证实际安装发现。Codex 通常会实时发现 Skill；只有 monica-guide 仍不可用时才重启。然后使用 $monica-guide 对此仓库执行 `init --release-tag {{MONICA_IMMUTABLE_REF}} --agent codex`，展示完整的 dry-run 预览和 planDigest；在我批准预览前，不要应用计划或进行后续修改。如果 Guide 修改了 AGENTS.md，请在开发前新开一次 agent run，因为指令按 run 加载。",
      "claude": "使用 `npx --yes skills@1.5.21 add https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide -g -a claude-code -s monica-guide -y`，仅从不可变来源 https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide 全局安装具名 monica-guide Skill。使用 `npx --yes skills@1.5.21 ls -g -a claude-code` 验证实际安装发现。Claude Code 通常会实时监视既有 Skill 目录；只有 monica-guide 不可用，或本会话启动时顶层 Skill 目录不存在时才重启。然后使用 $monica-guide 对此仓库执行 `init --release-tag {{MONICA_IMMUTABLE_REF}} --agent claude-code`，展示完整的 dry-run 预览和 planDigest；在我批准预览前，不要应用计划或进行后续修改。如果 Guide 修改了 CLAUDE.md 或 AGENTS.md，请在开发前新开会话，因为指令在会话启动时加载。",
      "generic": "使用 `npx --yes skills@1.5.21 add https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide -g -a codex -a claude-code -s monica-guide -y`，仅从不可变来源 https://github.com/Tairitsua/Monica/tree/{{MONICA_IMMUTABLE_REF}}/skills/monica-guide 全局安装具名 monica-guide Skill。使用同一 URL 和 `--list` 验证来源发现，再使用 `npx --yes skills@1.5.21 ls -g -a <agent>` 验证各目标的实际安装发现。只有宿主无法发现新 Skill 时才重启。调用 $monica-guide 对此仓库执行 `init --release-tag {{MONICA_IMMUTABLE_REF}} --agent codex --agent claude-code`，展示完整的 dry-run 预览和 planDigest，并在应用计划或进行后续修改前等待我的批准。"
    }
  }
}
