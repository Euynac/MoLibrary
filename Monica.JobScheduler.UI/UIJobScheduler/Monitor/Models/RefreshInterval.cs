namespace Monica.JobScheduler.UI.UIJobScheduler.Monitor.Models;

/// <summary>
/// Refresh interval options for the realtime monitor page.
/// </summary>
public enum RefreshInterval
{
    ThreeSeconds = 3000,
    FiveSeconds = 5000,
    TenSeconds = 10000,
    ThirtySeconds = 30000
}
