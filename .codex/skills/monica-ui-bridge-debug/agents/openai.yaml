interface:
  display_name: "Monica UI Bridge Debug"
  short_description: "Debug Monica UI through a bridge service"
  default_prompt: "Use $monica-ui-bridge-debug to inspect and refine a Monica UI module through a user-provided bridge service."
