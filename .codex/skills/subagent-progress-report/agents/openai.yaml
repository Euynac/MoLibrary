interface:
  display_name: "Subagent Progress Report"
  short_description: "Manage sub-agent session folders, logs, and status scans."
  default_prompt: "Use $subagent-progress-report to coordinate a main-agent session folder, sub-agent logs, and status scans."
