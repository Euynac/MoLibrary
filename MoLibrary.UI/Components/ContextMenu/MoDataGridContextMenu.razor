@* 
此组件已废弃，请直接使用 MoContextMenu 组件。

使用示例：
<MoContextMenu TItem="YourType"
               Items="@menuItems"
               Visible="@isVisible"
               InitialX="@x"
               InitialY="@y"
               ContextItem="@item"
               OnItemClick="@OnMenuItemClick"
               OnClose="@OnMenuClose" />
*@

@{
    // 这个组件已被废弃，不再提供任何功能
    // 请更新您的代码以使用新的 MoContextMenu API
}